js8  打印1到100之间所有数的总和与平均值
js9  求1到100之间所有偶数和
js10 求100以内7的倍数的和
js11 使用for 循环打印九九乘法表  解题思路 : 使用一个空的字符串来惊醒拼接
js12 接收用户输入的用户名和密码 , 若用户名为'admin' , 密码为'123456',则提示用户登录成功 ! 否则一直让用户输入 ;  使用do while 循环做判断,while使用!== 做判断 ,如果成立则返回上面做循环,如果是想让用户输入的就退出循环
js13 求1到100的累加值 , 但要求跳过所有个位为3的数;;;
js14 数组的创建
js15 数组的遍历
js16 遍历数组,并求出和与平均值
js17 遍历数组 ,并求出最大值  解题思路 : 遍历所有数组,使用一个数据进行记录
js18 数组转换为分割字符串
js19 新建数组 里面包括1~10
js20 筛选数组, 并将大于10的数值放到另外一个数组中
js21 数组去重   解题思路 :当取到要删除的数值时 , 使用数组名点splice(要删除数据的位置,要删除几位数据)的形式来进行同一数组删除操作
js22 数组的翻转 解题思路 :定义一个新数组来承接需要翻转数组下的数据,旧的数组从最后向前减,新的加加..
js23 冒泡排序   解题思路 : 定义一个数组 (废话 ,哈哈) 使用双重for循环, 外层判断需要外层循环的次数 , 内层循环决定单行判断的次数,在内层循环中定义一个临时变量, 使用if判断如果后面的小于前面的就做交换...
js24 函数简单运行