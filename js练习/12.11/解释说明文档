使用数字向boolean 类型转换
js2 循环
js3 循环练习 : 使用for循环打印01到50之间的数,每到10时换行 : 解题思路 : 初始定义三个变量,两个为内部变量,不用初始化数值, 一个为承接字符串的变量,将其赋值空的字符串即可.. 其中当数值小于10 时外层循环为0;所以外层循环初始值为0,小于5, 而内部为1到10 ,所以内部初始值为1 ,小于11 ...外层循环与内层循环为字符串拼接,在内部做判断 ,当内层循环数值为10时则与a+1相乘,否则就依次拼接 在外层for循环中进行换行,在拼接过程中使用\t进行空格.最后输出;;同时注意,使用字符串拼接时 , 要在数值前加'' 后面再跟数值..
js4 作用同上 (脑残了 ,想上面的循环...)
